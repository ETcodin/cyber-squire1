# AWS Security Hub Tool - Environment Variables
# Copy this to .env and fill in actual values

# AWS Configuration (REQUIRED)
CD_AWS_REGION=us-east-1
CD_AWS_ACCESS_KEY_ID=your_aws_access_key_here
CD_AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# Optional: Limit findings per scan (default: 100)
CD_MAX_FINDINGS=100

# Note: The AWS IAM user/role needs these permissions:
# - securityhub:GetFindings
# - securityhub:DescribeHub
#
# Minimal IAM Policy:
# {
#   "Version": "2012-10-17",
#   "Statement": [{
#     "Effect": "Allow",
#     "Action": [
#       "securityhub:GetFindings",
#       "securityhub:DescribeHub"
#     ],
#     "Resource": "*"
#   }]
# }
